# Dependencies
node_modules/

# Hardhat build artifacts
artifacts/
cache/
typechain-types/

# App Kit (Path A) build output — source lives in appkit/, build via npm run build
appkit/dist/

# Local deployment state (contains addresses, not secrets)
deployments.json

# Environment / secrets
.env
.env.local
# NOTE: jangan pakai wildcard `.env*` — itu meng-ignore `.env.example` (tracked).

# Editors / OS
.DS_Store
Thumbs.db
*.log

.vercel
.mcp.json

# Worktrees & docs (operational runbooks stay local-only — never commit)
PekerjaanAgents/
DEPLOY.md
IDEA.md
MAINNET-CHECKLIST.md
ARC-SOURCES.md
.commandcode
ARC-REFERENCE.md

# Agent skill packs (installed via npx skills add; global mirror at ~/.agents)
.agents/
.claude/
agent/
skills-lock.json


# Backend runtime data (events + watermarks + relayer jobs — local only)
backend/data/
